const project = Vue.component('project', {
    props: ['project'],
    template: '<div class="project_tile">{{ project.name }}</div>',
    computed: {

    },
    watch: {

    },
    methods:{

    }
});
